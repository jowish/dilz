# Agent metrics

One line per merged agent PR. Fill it at merge time — retrofitting this later is impossible,
and without it the whole workflow is an anecdote instead of a track record.

## Log

| PR | Issue | Size | Opened | Merged | Cycle time | Human edits after PR (lines) | CI reds before green | Rework issue opened? | Est. API cost | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| #  | #  | S/M/L |  |  |  |  |  | yes/no |  |  |
| #13 | #9 | S | 2026-08-09 | 2026-08-09 | ~2h (incl. infra debugging) | 1 (assertion in bottomNavContract.test.mjs, agent-authored) | 1 (Vercel prix-israel unrelated) | no | via subscription | Agent halted correctly on a pinned test value per AGENTS.md, asked before rewriting the assertion |
| #14 | #11 | S | 2026-08-09 | 2026-08-09 | ~10min | 0 (docs only) | 0 | no | via subscription | Docs-only cleanup, closes #11. Discovered PromoModal.js + scan.js price-comparison UI is still live in prod (not dead code as assumed) — needs a product decision, not just docs. |
| TBD | #15 | M | 2026-08-09 |  |  |  |  |  | via subscription | Scope widened during implementation with explicit user sign-off: PromoModal/PromoCard/scan.js were wired into a whole price-comparison surface (promos fetch, sales tab, filters, voting) beyond the two files named in the issue title — removed the full surface, not just the modal render, to avoid leaving dead click targets. PR number pending (no gh auth in this environment to open it). |
| TBD | #18 | S | 2026-08-09 |  |  |  |  |  | via subscription | Deleted the 5 files confirmed orphaned by #17's investigation (pages/api/promos.js, product-votes.js, prix.js, scripts/import-promos.js, traduire-produits.js). No dedicated test files existed for any of them. Also removed their now-dead `node scripts/...` lines from README's Legacy section as a direct consequence. tests/criticalRegressionGuards.test.mjs unaffected — none of the 5 files were referenced there. PR number pending (no gh auth in this environment to open it). |
| TBD | #35 | S | 2026-08-30 |  |  |  |  |  | via subscription | Added a profile icon and a notifications/alerts icon to AppHeader.js so desktop has a click path to /profil (theme, sort, language, delete account) and /alerts, matching BottomNav.js's existing routes/handlers exactly. Reused the pre-existing `.dilz-app-header__right .dilz-icon-button { display: none }` mobile-only CSS rule instead of adding a new one, so BottomNav.js/GlobalBottomNav.js needed zero changes. NotificationSheet.js was flagged in the PR as still unwired (out of scope per the issue) rather than built now. PR number pending (no gh auth in this environment to open it). |
| TBD | #37 | S | 2026-08-30 |  |  |  |  |  | via subscription | Installed @vercel/analytics, mounted `<Analytics />` once in pages/_app.js. Confirmed no cookie-consent UI is needed (Vercel Analytics is cookie-free) and added a disclosure sentence to pages/privacy.js's existing "Service providers" section (en+he) since it previously didn't mention analytics at all. Recommended in the PR, not added: @vercel/speed-insights as a separate follow-up issue. `npm install` incidentally deduplicated a pre-existing duplicate `"check"` script key already on main (harmless, identical value both times) — left as-is rather than reverting. PR number pending (no gh auth in this environment to open it). |
| TBD | #39 | S | 2026-08-30 |  |  |  |  |  | via subscription | Wired the pre-built NotificationSheet.js to both bell icons: GlobalBottomNav.js now owns the notifications list + sheet-open state and opens it directly on mobile's Alerts tap; AppHeader.js (desktop, no shared state with GlobalBottomNav) dispatches a new `dilz:open-notifications` window event that GlobalBottomNav listens for — same bridge pattern the codebase already used for `dilz:notifications-read`. Reused the pre-existing `alertsOpen` BottomNav prop (previously redundant with route-derived activeTab) to keep the liquid-nav bubble animation correct while the sheet is open without navigating. Clarified in the PR: the sheet complements /alerts, it does not replace it — AlertModal.js's own "Alert results" section (with its own mark-all-read) is untouched and still the place to manage saved alert criteria; the sheet is a no-navigation quick-glance. No new notification-sending logic added, per the issue's constraint. Updated 3 pre-existing test files whose assertions locked in the old (pre-#39) bell-tap behavior; added a new dedicated test file for the sheet-wiring contract. PR number pending (no gh auth in this environment to open it). |
| TBD | #41 | S | 2026-08-30 |  |  |  |  |  | via subscription | Confirmed OG tags were per-deal in the JSX already but never reached a non-JS crawler: pages/deal/[id].js had no data-fetching method at all, so `deal` state started null and only populated client-side after hydration — the `<Head>` block with real og:title/og:description/og:image only ever rendered after an `if (!mounted) return null` gate, and `mounted` is always false during SSR. Confirmed via `npm run build` output before the fix: `/deal/[id]` was marked `○ (Static)`, meaning every deal shared the exact same generic prerendered shell regardless of id. Added `getServerSideProps` (fetches the deal server-side, same query as the existing `/api/deal/[id]` route) and hoisted the Head/OG computation above the mounted/loading/not-found gates so it always renders, seeded from the SSR data. Verified empirically: `npm run build` now shows `ƒ /deal/[id]` (server-rendered per request), and `curl`ing a running `next start` instance shows real og:title/og:description directly in the raw HTML with zero JS execution. Could not verify a *real* deal's title/price/photo from this workstation (no live Supabase credentials here) — recommended in the PR that a human run the URL through Facebook's Sharing Debugger or WhatsApp's own preview after a real deal is live on the Vercel preview. One file touched (pages/deal/[id].js) plus one new test file, matching the issue's stated scope. PR number pending (no gh auth in this environment to open it). |
| TBD | #45 | M | 2026-08-30 |  |  |  |  |  | via subscription | Stopped and asked the user before writing code: the issue assumed two "existing thresholds" to reuse (a deal-score "hot" cutoff, a comment N-upvotes cutoff) that turned out not to exist anywhere — `tri=hot` is a sort order not a score cutoff, and `commentaires` has no vote column or vote endpoint at all. User decided: pick and clearly document a new votes_chaud cutoff (chose 10, +10 points, flagged as new/not-reused in code comments and here), and skip comment-upvote points entirely as a separate follow-up issue rather than build new comment-vote schema beyond the migration this issue authorized. Added `supabase-user-points-setup.sql` (new `user_points` table, no INSERT/UPDATE/DELETE grant to authenticated/anon — server-only writes) following the exact convention from #31's precedent migration. `lib/points.js` recomputes (not increments) a user's total from current deal votes on every vote cast in pages/api/bons-plans.js, avoiding a double-award bug an incremental approach would have needed a second bookkeeping column to prevent. Unit tests for `pointsToTier` caught a real off-by-crash bug on negative input before it shipped. Updated one pre-existing test (dealPromotionContract.test.mjs) whose assertion locked the old `.select('is_ad')` shape, now `.select('is_ad,auteur_id')`. PR number pending (no gh auth in this environment to open it). |
| TBD | #47 | S | 2026-08-30 |  |  |  |  |  | via subscription | This gap was originally surfaced as a finding during #45's follow-up review, then turned into its own issue. Added one entry ("View my public profile" → `/user/${user.id}`) to ProfileTab's existing "Quick links" array in pages/index.js — no new component needed, since ProfileTab is the exact same component reached by both the mobile BottomNav profile tap and the desktop AppHeader profile icon (#35/#39), so "works on both mobile and desktop" was automatic rather than something to build twice. Confirmed no CSS hides `.dilz-profile-tab`/`.dilz-profile-links` at any viewport width. No changes to pages/user/[id].js itself, per the issue's constraint. PR number pending (no gh auth in this environment to open it). |
| TBD | none (user-provided, no issue) | M | 2026-09-01 |  |  |  |  |  | via subscription | Replaced the procedurally-drawn placeholder "d" mark with the user-supplied final app icon artwork (saved as assets/source/icon-master.png), regenerating every iOS/Android/web icon and splash size from it via scripts/generate-app-icons.js. Rewrote the generator: it no longer draws vector geometry, it resizes the raster master directly for full-bleed icons and extracts a standalone tinted glyph (luminance-as-alpha cutout) for cases needing the mark alone at a different scale/colour — Android adaptive foreground, maskable web icon, iOS/Android splash marks. favicon.svg is now an SVG wrapper embedding the raster master (data URI) since there is no vector source to redraw at arbitrary size. Touches only public/, assets/, ios/App/App/Assets.xcassets/, android/app/src/main/res/, and scripts/generate-app-icons.js — no capacitor.config.*, no native code, no signing. `npm test` (485/485) and `npm run build` both pass. No GitHub issue existed for this; user confirmed proceeding without one. |
| TBD | none (user-provided, no issue) | S | 2026-09-01 |  |  |  |  |  | via subscription | Priority 1 of a 5-part UI-quality pass. Deal card product photos (often plain white product-on-white-background photography) sat as a raw bright block against the dark theme's card. Added a soft inner ring (`box-shadow: inset 0 0 0 1px`) on the image itself plus an edge vignette via a `.dilz-deal-card__media::after` pseudo-element (radial-ish inset shadow, stronger in `.dark`), reusing the existing `--surface-soft`/`--bg-soft` media backdrop rather than inventing new tokens. Deliberately did not touch `object-fit` or crop behaviour — pure overlay, so the image stays exactly as visible/undistorted as before. Verified with a static harness (real `.dilz-deal-card` markup + globals.css + a synthetic white-bg product photo, served locally and screenshotted via Playwright) since this dev environment has no Supabase credentials to load real deals — confirmed a visible before/after difference in dark mode, confirmed no visual regression in light mode, and confirmed RTL (`dir="rtl"`) is unaffected since the change uses no direction-dependent properties. `npm test` (485/485) passes; `tests/criticalRegressionGuards.test.mjs` doesn't reference this selector so wasn't at risk. |
| TBD | none (user-provided, no issue) | M | 2026-09-01 |  |  |  |  |  | via subscription | Priority 2 of the same 5-part UI-quality pass. Replaced the 🔥/❄️ emoji used for hot/cold votes with vector icons matching the app's existing SVG icon language (24×24 viewBox, `currentColor`, same convention as `BottomNav.js`/`CommentIcon`/`ShareIcon`). Fixed at the single shared `components/ui/VoteEmoji.js` component, which turned out to already be the one thing rendering hot/cold everywhere live (DealCard, PromoCard, deal detail, map, profile, user profile, bons-plans-shopping) — 9 call sites fixed from one file. Gave the icon its own semantic colour class (`--hot`/`--cold`) so hot vs cold stays recognisable even when unvoted (previously only the native emoji glyph carried colour; a plain currentColor icon would otherwise default to gray). Caught and fixed a real contrast bug while verifying: a pre-existing sitewide `!important` rule fills a *voted* button solid brand-orange (hot) or navy (cold) with forced white text — my new icon color class was fighting that and rendering as an orange flame on an orange fill, nearly invisible; added a targeted override so the icon inherits the button's forced white specifically in the voted state. Per the issue's "keep the same orange accent for hot" instruction, changed the deal-card combined pill's hot colour from a red (`#DC2626`, never actually matched the brand) to `var(--brand)`, and kept/darkened the existing blue for cold with a `.dark` override for contrast. Also swapped two decorative emoji (🔍 search, 🤷 no-results) in pages/index.js's search tab for small inline SVGs in the same style — the only other emoji found in code that reachable, live UI actually renders. Did NOT touch: `lib/translations.js`'s `shareNewDeal` string (📸) — grepped for its usage and found it dead, never rendered anywhere; `pages/bons-plans.js` (🔥📸🛍️📍💬🕒📅) — grepped every route/link in the app and found nothing navigates a user to `/bons-plans` (only self-references and a route-highlight config entry), so it appears to be orphaned pre-redesign code with its own hardcoded hex colours outside the token system entirely; flagging both for a human product decision rather than silently touching unreachable code, per AGENTS.md §5 (don't widen scope) — same pattern as #14's PromoModal.js finding. Verified light/dark/RTL with a static harness (real markup + globals.css, screenshotted via Playwright) covering all vote-pill states (unvoted/voted-hot/voted-cold), profile stat cards, and the two search empty states. `npm test` (485/485) and `npm run build` pass. |
| TBD | none (user-provided, no issue) | M | 2026-09-01 |  |  |  |  |  | via subscription | Priority 3 of the same 5-part UI-quality pass: feed information density. Measured the real problem first rather than guessing from the CSS source (this stylesheet has several duplicated rule blocks for the same selector at different line numbers, so a plain grep-and-edit risked landing on a copy the cascade doesn't actually use) — mocked `/api/bons-plans` via Playwright route interception so the real `DealCard` component rendered with real data at a 390×844 mobile viewport, then measured actual card-to-card cycle height. Baseline: 1.42 cards/screen (card cycle 495.78px content height 705px), matching the issue's "~1.5" estimate closely. Iteratively tightened the default "card" layout only (list/compact/spotlight already have their own tighter layouts and were deliberately left untouched — confirmed unaffected by screenshotting spotlight afterward): shortened the photo panel from a 16:10 to a 2.9:1 aspect ratio, trimmed non-interactive margins/padding throughout the body (store row, author line, price row, meta row, card and grid gap), clamped the description preview from 2 lines to 1, and shrank the price badge's own padding/font-size slightly (it's a label, not a tap target). Landed at 2.07 cards/screen (cycle 341px) — inside the requested 2–2.5 range. Did not touch any interactive element's size: vote buttons (26×26), share button (40×40) and save button (27×27) measured identical before/after — note these were already below the 44px tap-target floor before this change; leaving that as-is since raising it was out of this issue's scope. New rules appended as a high-specificity (`:not(.is-list):not(.is-compact):not(.is-spotlight)`) block at the end of globals.css rather than editing the scattered originals in place, for the cascade-certainty reason above. Verified light/dark/RTL (Hebrew) with the same mocked-feed harness; `npm test` (485/485, including `criticalRegressionGuards.test.mjs`) and `npm run build` pass. |
| TBD | none (user-provided, no issue) | S | 2026-09-01 |  |  |  |  |  | via subscription | Priority 4 of the same 5-part UI-quality pass: consolidate profile stat cards. `.dilz-profil-stats`/`.dilz-stat-card` turned out to be a single shared component used on two pages — `pages/profil.js` (3 stats: deals posted, hot votes, cold votes) and the public `pages/user/[id].js` (6 stats: deals, followers, following, hot votes, cold votes, last post) — so one CSS fix covers both. Redesigned the container from a 2-column grid of separately bordered/backgrounded cards to a single bordered/backgrounded strip with hairline dividers between cells (vertical between columns, horizontal where a 4th+ stat wraps to a second row) — zero JSX changes on either page, same class names, same DOM shape. Caught one real conflict while verifying: dark mode had `.dark .dilz-stat-card { background: #181C26 }` painting each cell's own dark background, which would have defeated the "one shared surface" redesign by putting the background back on individual cells — moved that declaration to `.dark .dilz-profil-stats` instead (same color, correct element). Verified with a static harness (real markup for both the 3-stat and 6-stat versions + globals.css, screenshotted via Playwright) in light, dark, and RTL — confirmed the column dividers land correctly in RTL despite grid column order reversing. Neither page requires network mocking to verify since no JSX/data-shape changed, only presentation. `npm test` (485/485) and `npm run build` pass. |
| TBD | none (user-provided, no issue) | S | 2026-09-01 |  |  |  |  |  | via subscription | Priority 5 (final) of the same 5-part UI-quality pass: floating-layer overlap. Found the actual root cause via computed-style inspection rather than guessing: the mobile floating "liquid glass" bottom nav (`.dilz-bottom-nav`, inside a media query) has `z-index: 1000 !important`, while `.dilz-sheet-overlay` — the shared full-screen modal backdrop used by `NotificationSheet.js` and 3 other consumers (`CityModal.js`, `PromoModal.js`, the deal detail page's sheet) — was only `z-index: 400`, so the nav rendered on top of a supposedly-modal sheet instead of being covered by it. Fixed by raising `.dilz-sheet-overlay` to `z-index: 1100` (above the nav's 1000 and `.dilz-safety-modal`'s 1000, still well below the transient startup-splash's 10000) — the nav now visually recedes fully behind the dark scrim while any of these sheets is open, satisfying the issue's "either the nav recedes" option without adding any JS show/hide logic. One-line CSS change with no duplicate rule to worry about (grepped — only one `.dilz-sheet-overlay` definition exists). Verified by reproducing the exact bug first (temporarily reverted the z-index, screenshotted the nav visibly poking through the sheet's bottom edge — confirms this was real, not cosmetic) then re-screenshotting after the fix in light, dark, and RTL, all via a Playwright harness that opens the real `NotificationSheet` in the real running app by dispatching the same `dilz:open-notifications` event `AppHeader.js` uses, sidestepping the need to fake a full Supabase auth session. Noted but did not touch: `.dilz-main-menu__backdrop` (a different, separate modal system for the hamburger menu) sits at `z-index: 900` — also technically below the nav's 1000, but it already intentionally carves out space above the nav via `inset: 0 0 calc(96px + ...)` rather than covering it, a different (deliberate-looking) design; left alone as out of scope since the issue named the notifications sheet specifically. `npm test` (485/485) and `npm run build` pass. |
## Derived KPIs — recompute monthly

| KPI | Definition | Current | Target |
|---|---|---|---|
| First-pass merge rate | % of agent PRs merged with zero human code edits |  | > 60% |
| Median cycle time | Issue labelled `agent-ready` → merged |  | < 24h |
| Rework rate | % of merged PRs that produced a follow-up bug issue within 14 days |  | < 15% |
| Restricted-scope violations | PRs blocked by the CI guard |  | 0 |
| Cost per merged feature | API spend / merged PRs |  | track only |
| Escaped defects | Bugs reaching the deployed app, per month |  | flat or down |

## Why these and not others

- **First-pass merge rate** is the only number that separates "I used an AI assistant" from
  "I operated an agentic workflow". It is the headline figure.
- **Rework rate** and **escaped defects** are the honesty check: throughput that ships bugs is
  not throughput. Reporting them is what makes the other numbers credible.
- **Restricted-scope violations** demonstrates that the guardrails were designed, not improvised.

## Baseline

Before the first agent PR, record how long a comparable change took when done by hand.
Without a baseline, "faster" is not a claim you can defend in an interview.

| Reference change | Done by | Time |
|---|---|---|
|  | human |  |
